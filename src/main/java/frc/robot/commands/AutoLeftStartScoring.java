package frc.robot.commands;
import com.pathplanner.lib.commands.PathPlannerAuto;
import edu.wpi.first.wpilibj2.command.*;
import frc.robot.subsystems.*;

public class AutoLeftStartScoring  extends SequentialCommandGroup{
   // public  AutoLeftStartScoring(){
        //Moves Robot backward 
     //   addCommands(new PathPlannerAuto("Move Back, Rotate"),
        
        //RD: Shoot all 8 balls in Hopper! I Hope
        /* 
        new WaitCommand(4.5).deadlineFor(AutoCommands.Shoot(Shooter.getInstance(), Index.getInstance(), .75, .4)),
        
        AutoCommands.Shoot(Shooter.getInstance(), Index.getInstance(), 0, 0))        
        
        );
    
*/


    }
//}
